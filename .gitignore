# .NET
bin/
obj/
TestResults/
*.user
*.suo
.vs/
*.DotSettings.user

# Runtime output
logs/

# IDE / OS
.idea/
.DS_Store
*.swp

# Local secrets never belong here; use `dotnet user-secrets` (see README)
appsettings.*.local.json
google-services.json
# OAuth client downloads from Google Cloud: their values go into user-secrets, the files stay out
client_secret*.json

# Deployment: real values and data never leave the server
deploy/.env
deploy/secrets/
deploy/backups/
